import { ImageData } from "../Image/types"

export type projectData = {
    title: string,
    labels: string[],
    description: string,
    appLink: string,
    webLink: string,
    icon: string,
    image: ImageData
}